
# ## Como meter un archivo  -----------------------------------------------

library(redmis)
"nombre" surce data(url)
# en caso de que no aparesca el archivo revisar con este comando: view(nombre del archivo )

#para importar datos no seguros se usa els siguiente comando <-read.csv

#comando para la estadistica descriptiva 
#Prueba t de Student (t-test)...... maneja dos variables 
t.test(variable ~ grupo, data = datos)
# tapply 
#para saber la normlidad tomamos en cuenta el valor de p 




#Anova... cuando tenemos mas de 3 observaciones que comparar 